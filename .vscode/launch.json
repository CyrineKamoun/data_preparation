{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Data Preparation CLI: manage.py",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/manage.py",
            "console": "integratedTerminal",
            "args": [
                "--actions", "",
                "--datasets", "",
                "--region", "",
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}
